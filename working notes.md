<!-- Enable the ability to widen or narrow the frame on the left-hand side of the UI that contains the instructions and preview site in each level. The data layer events tool towards the bottom of the preview site should also be capable of being pulled up by the user so they can see more of the logged history. 
The preview site has a section in a yellow background that's located beneath the header banner and above the list of products. It says "Simulate user type" and allows the user to choose between external or internal employee and then gives the data layer user type value. It's not clear what this feature should be used for. Either make it more clear what its use case is, or remove it entirely if it's unnecessary.
When clicking around the preview site, the Front end doesn't change visually between home, products, and about pages.
The instructions for level one challenge one should have its objectives ordered with "Create a page view trigger" first, since I've noticed the tag cannot be saved without applying an existing trigger to it.
When creating a new trigger, the variable and operator drop-downs are Overlapping a little bit.
It's helpful to be able to reference the instructions when building a new trigger, tag, or variable, so make an adjustment so that the background isn't Blurred when these frames are opened.
Adjust the instructions for challenge two of level one to have the user craft the trigger conditions to be "click text equals Add to Cart" instead of "click element contains .add-to-cart".
The Instructions pane needs to have a scroll feature. E.g. When hints are available, I'm noticing I can't view all of them because I can't scroll down. 
For challenge three of level one, offer a suggested name for the data layer variable in step one of how to complete that which will match the name of the variable mentioned in step two.
It would be cool if all tags, triggers, and variables created in prior challenges persist through future challenges (i.e., are always visible in the tags, triggers, and variables section of the UI). For example, challenge three of level one asks the user to update the GA4 configuration tag that was created in challenge one of level one, But to do so, the user has to recreate that tag rather than select it from a list of existing tags to edit.
It would be cool if, when the user clicks the check work button, a pop-up appears informing the user which of the objectives have been completed correctly and which of the objectives have not been completed correctly. For those objectives that haven't been completed correctly, it will Offer instructions in that pop-up as to how the user can complete each step that's left.
It would be neat if the user, after building the tags, triggers, and/or variables assigned in the challenge, had to actually navigate the preview site to force the tag trigger and/or variable they created to fire or execute before they could advance to the next challenge. This gives them immediate feedback to see how what they created actually functions.
In general, the UI feels like it could be organized better. For example, I like how the instructions are constantly visible on a left-hand pane, but site tagging is all about working side-by-side between the tag management system and the website, however, this UI has what it looks like 80% of its display occupied by the GTM workspace simulator and only a small column on the left side available to preview the website when the user navigates to it. I'm thinking there should be room for all three of these things to be permanently visible in the UI: the instructions, the preview site, the GTM workspace.
For challenge number one of level two, the user should be instructed to hover over the email field in the preview site to reveal the CSS selector value for that field, And to Use that value as the value for the CSS selector condition when creating the DOM element variable.
It would be ideal if the iframes that pop up where the user builds the tag, trigger, and variable looked more similar to those templates in GTM (but including only the relevant components from those templates)
It would be neat if technical terms in the instructions pane functioned in a way where if the user hovers over them a small window opens with text that explains the term deeper. E.g. level 2-2 says "Their dev team fires a custom event 'purchase' through the data layer along with transaction data." In this sentence, the term "custom event" could expand to show text that explains how this is not referring to the custom event tag type in GTM but rather a parameter embedded in the source code of the website by its developer to empower analytics initiatives to ingest more structured data.
for challenge 2-2, the outlined steps are missing some key information for the student. Step 1 instructs to create a data layer variable and the name of it, but doesn't specify what to insert as the value. Step 2 instructs the student to create a custom event trigger, but doesn't specify the conditions they should use. I'm highlighting challenge 2-2 as an example, but we need to make sure the steps are dummy-proof across all challenges, as this material might be very new to some users.
At the end of the 3 tiers of 3 challenges each, it would be effective if we had the student answer a few questions to drive home some learnings. These can be any format, but i'm thinking predominantly multiple choice, where an explanation is offered after the answer is highlighted (whether or not the student selected it correctly). These questions serve as our opportunity to highlight critical points about GTM that we want to make sure the student walks away understanding. For now i don't want the enforce a minimum score for the user to pass this section. There should be 5-10 questions. I'll offer two suggestions to start, then i want you to think of the rest and i'll let you know if i approve them.  My first suggestion: Select all the URLs that would satisfy the following Page View trigger condition: Page Path contains company (A) https://www.website.com/company (B) https://www.website.com/company-trips (C) https://www.website.com/vacations?utm_campaign=company (D) https://www.company.com/about?utm_source=news ANSWER: A, B. EXPLAINER: The page path is the portion of the URL after the top-level domain (.com) and before the query string (?utm_source=news). Unlike 'equals', the 'contains' operator is non-exclusive, meaning the page path only needs to feature the 'company' value but can have unlimited other text. My second suggestion: Match the definition to the appropriate part of the URL. https://www.website.com/about-us/company?utm_source=news&utm_campaign=ads#definition. (A) Protocol (B) Host (C) Sub Domain (D) Main Domain (E) Top-level Domain (F) Path (G) Query string (H) Fragment
Challenge 3-1 tasks the student to first create two data layer variables then create a page view trigger in which the condition starts with one of those variables by name. The first step never specified exactly what the student should name the variable. Therefore, if they didn't name it correctly in their first step, they would have to go back to edit the variable name to satisfy step 2.
if a variable or trigger or tag field that is optional is supposed to be left blank, specify that in the how-to-complete instructions.
Challenge 3-3 the student needs to build triggers using the variables they just created. Specify in the how-to exactly what they should enter into which field for building these triggers. -->
<!-- i'm liking the hover windows for technical terms in the objectives pane in each level, but they are being cut off by the neighboring preview site window. They should hover over everything.
When a trigger that the user previously created is selected when building out a tag, the name of the trigger seen in the dropdown changes to an encoded string of alphanumeric characters the moment it is selected. It should always display its user-labeled name.
The text for each option in the Tag Type dropdown inside the window where the user builds tags is cut off. The size of the dropdown window will need to be widened.
The "Data Layer Events" window at the bottom of the app shows the user a log of DL events occurring as they navigate the test site, but it doesn't show whether or not the tag they setup succesfully fires or not like a GTM debugger window would. This window should be changed from being only a live feed of DL events to a live feed of DL events + tags firing when trigger conditions are successfully met.
The AdOps team that we're primarilyt designing this app for will be most often implementing CM360 Floodlight activity tags over any other type of conversion tracking tag. The first three challenges in Level 1 should be revised. Challenge 1 can have the user implement a universal Google Tag associated to the CM360 Advertiser's Floodlight configuration instead of a universal GA4 Configuration tag. Challenge 2 can be a Floodlight activity instead of a GA4 event. Challenge 3 can be the same but altering the trigger for the Google Tag instead of the GA4 config tag.
Challenge 2-1, if the user must type variables into the parameter value field surrounded by squiggly brackets, then this needs to be specified in that step under the "How to Complete" section.
Challenge 2-2, "How to Complete" step 2 bullet says "This fires when developers push { event: 'purchase' } to the data layer" - This should be changed to "This fires whenever { event: 'purchase' } surfaces in the data layer"
Challenge 2-3, under Hints section: "The Conversion Linker must always fire before Google Ads tags on the same page." should be changed to "The Conversion Linker should always fire before any Google conversion tags on the same page."
Challenge 2-3, under Hints section: "The "Setup Tag" field in Google Ads tags is how you configure the firing order in GTM." should be changed to "Firing order is configured in GTM under the "Advanced Settings -> Tag Sequencing" section of any tag, where a tag can either be set to fire before or after a specified tag."
Challenge 2-3, How to Complete step 3 instructs the user to "Set Setup Tag (fires before this tag) → select your Conversion Linker tag" but the tag building template is missing a "Setup Tag" section that would allow the user to do this. Make this feature available in the tag building template, but instead of being labeled "Setup Tag" make two checkboxes available just like in GTM, one labeled "Fire a tag before specified tag fires" and the other "Fire a tag after specified tag fires" with a field beside each that allows the user to specify a tag name.
Change "Hints" to "Tips" in each level.
Level 3-1, CHeck Work Results says GA4 and Google Ads tags are not linked to their associated consent triggers even if the user does this properly. This needs to be fixed in the game logic. It's possible the reason for the mixup is because a GA4 Configuration tag was already created earlier in the game, so now there are two, although this won't be the case in the next version since we have changed the first level to use floodlights/google tags instead of GA4 tags.
Apply some of the underline definition hover features to technical terms in the level overview sections as well, particularly level 3-1 with terms about consent and GDPR.
Level 3-2 HOw to COmplete step 1 mentions "Fix: change the condition variable to reference userType" which should read "- Fix: change the condition variable to reference dlv_userType"
Level 3-3: How to Complete step 1 says "Devs push this with every interaction (e.g. { channel: 'paid_search' })" which should instead say "Devs have coded the data layer to surface this channel variable with every event (e.g. { channel: 'paid_search' })"
Level 3-3: How to Complete step 4 asks the user to create a Custom Event trigger for the Purchase event and step 5 asks the user to create a Purchase GA4 Event tag, but these were already created earlier in the game and are still available for the user to reference in their GTM Workspace section. Step 4 should be removed, while step 5 should be rephrased to ask the user to "Revise the existing GA4 Event Tag for the Purchase event" and the only bullet is to add the additional custom dimension.
GTM Knowledge Check, question #5: Answer A (Give the COnversion Linker a higher priority number in the tag settings) is technically also a viable way to ensure the Conversion Linker tag fires before the other tag (if the have the same trigger). Add "Two correct answers." to the end of the question and require the user to select both A and B.
GTM Knowledge Check, question #5: As discussed in point #30 in this working notes.md file, "Setup Tag" is misleading information as to where the user would define tag firing priority. Revise the phrasing of this answer to instead read "Set the Conversion Linker as the tag associated to the 'Fire a tag before specified tag fires' setting in the Google Ads Tag Sequencing section. -->